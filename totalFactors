
function solution(N) {
    // write your code in JavaScript (Node.js 8.9.4)
    let count = 0;
    for(let i = 1; i <= N; i++){
        if(N%i==0){
            count = count +1;
        }

    }
    return count;
}
